# DATABASE_URL is the PostgreSQL connection string used by the backend.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# PORT is the HTTP listen port injected by runtime.
PORT=8080

# APP_PORT is a fallback HTTP listen port when PORT is absent.
APP_PORT=8080
