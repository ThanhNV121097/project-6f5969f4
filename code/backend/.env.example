# PostgreSQL connection string injected by runtime or compose.
DATABASE_URL=postgres://hello:hello@db:5432/hello_word?sslmode=disable

# HTTP listen port.
PORT=8080

# Optional fallback HTTP listen port if PORT is unset.
APP_PORT=8080
